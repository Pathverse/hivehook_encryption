name: example
description: "Hivehook Encryption demo with flutter_secure_storage"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hivehook: 1.0.0-alpha.2
  hihook: ^0.1.0
  hivehook_encryption:
    path: ../
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
